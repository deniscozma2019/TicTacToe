{
  "name": "react-socket-app",
  "version": "1.0.0",
  "private": true,
  "description": "Simple React + Socket.io app",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others \\\"node server.js\\\" \\\"cd client && npm start\\\"",
    "first": "echo \\\"Installing server packages.. \\n\\\" && npm install && cd client && echo \\\"\\nInstalling client packages..\\n\\\" && npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "dev": "nodemon app.js"
  },
  "author": "",
  "license": "",
  "dependencies": {
    "concurrently": "^3.5.1",
    "express": "^4.16.3",
    "nodemon": "^2.0.22",
    "socket.io": "^2.0.4",
    "socket.io-client": "^2.0.4"
  }
}
